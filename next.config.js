/** @type {import('next').NextConfig} */
const nextConfig = {
      output: 'export',
    images: {
      domains: ["images.pexels.com", "backguy-publicc-photos-dev.s3.amazonaws.com"],
    },
  };
  
  module.exports = nextConfig;
